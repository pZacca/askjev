name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run typecheck
      - run: npm test

  smoke:
    needs: check
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run smoke

  # Every green push to main goes live on jev.zacca.dev.
  deploy:
    needs: [check, smoke]
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    concurrency:
      group: deploy-worker
      cancel-in-progress: false
    environment:
      name: production
      url: https://jev.zacca.dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm run deploy
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      - name: Verify the live endpoint
        run: |
          curl -sSf https://jev.zacca.dev/ | tee /dev/stderr | grep -q '"name":"askjev"'
      # Smithery keeps a snapshot of the tool taken at publish time, so re-scan the new Worker.
      - name: Refresh the Smithery listing
        run: npx --yes smithery@1 mcp publish https://jev.zacca.dev/mcp -n pzacca/askjev --config-schema smithery.schema.json
        env:
          SMITHERY_API_KEY: ${{ secrets.SMITHERY_API_KEY }}
