// Installed-tarball smoke test: npm pack, install into a temp dir, spawn the real executable,
// complete the MCP handshake, list tools, call `ask` against a stubbed upstream.
// Pending: lands with the router implementation.
console.error("smoke: not implemented yet");
